# Economy System Script\n\nextends Node\n\n# Variable to hold player credits\nvar credits : int = 0\n# Reward for each kill\nvar kill_reward : int = 100\n# Count of loss streaks\nvar loss_streak : int = 0\n\n# Function to add credits based on kills\nfunc add_kill_reward():\n    credits += kill_reward\n    print("Credits after kill: ", credits)\n\n# Function to handle losses and track loss streaks\nfunc handle_loss():\n    loss_streak += 1\n    if loss_streak >= 3:\n        print("Penalty applied due to loss streak!")\n        # Apply penalty or reset mechanism here\n    print("Current loss streak: ", loss_streak)\n\n# Reset loss streak after a round\nfunc reset_loss_streak():\n    loss_streak = 0\n    print("Loss streak reset.")\n\n# Function to manage economy per round\nfunc manage_round_economy():\n    print("Managing round economy...")\n    # Logic to handle round-based credits and rewards\n